<%= form_for(@mailsent) do |f| %>
  <% if @mailsent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailsent.errors.count, "error") %> prohibited this mailsent from being saved:</h2>

      <ul>
      <% @mailsent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mailtemplate_id %><br>
    <%= f.number_field :mailtemplate_id %>
  </div>
  <div class="field">
    <%= f.label :sent_time %><br>
    <%= f.datetime_select :sent_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
